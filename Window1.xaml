<Window x:Class="radmin.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:radmin"
        mc:Ignorable="d"
        Title="Window1" Height="371" Width="359">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Button x:Name="startButton" Content="Start" Margin="5" Click="StartButton_Click" />
            <Button x:Name="stopButton" Content="Stop" Margin="5" Click="StopButton_Click" IsEnabled="False" />
        </StackPanel>

        <ListBox x:Name="logListBox" Grid.Row="1" Margin="5" />
    </Grid>
</Window>